<!--<h2>ここはbookのshowページです</h2>-->

<div class="container">
  <div class="row">
    <div class="col-md-3">
      <!--部分テンプレート内でインスタンス変数を定義すると、別のコントローラのアクションに記述された同名の-->
      <!--インスタンス変数があるビューに部分テンプレートを呼び出すと部分テンプレートで定義したインスタンス変数が-->
      <!--変わってしまい、エラーや不整合になってしまう-->
      <%= render "layouts/profile", user: @user , book: @book_new  %>
    </div>

      <div class="col-md-8 offset-md-1">
        <h2>Book detail</h2>
          <table class="table table-hover">
            <thead>
            </thead>
        <tbody>
        <td><%= image_tag @user.get_profile_image(100, 100) %><br><%= link_to @user.name, user_path(@user) %></br></td>
          <td><%= link_to @book.title, book_path(@book) %></td>
          <td><%= @book.body %></td>
        <!--特定の本を取得し、その本に関連付けられたユーザーオブジェクトを取得することで自分以外の投稿データは編集できなくする-->
        <% if @book.user === current_user %>
          <td><%= link_to 'Edit', edit_book_path(@book), class: 'btn btn-primary' %></td>
          <td><%= link_to "Destroy", book_path(@book), method: :delete, data: {confirm: "本当に消しますか？"}, class: 'btn btn-sm btn-danger' %></td>
        <% end %>
        </tbody>
          </table>
      </div>
    </div>
  </div>
</div>